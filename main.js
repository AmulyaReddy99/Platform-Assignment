window.onclick = function(event) {
    if (event.target == (modal = document.getElementById('abstract'))) {
        modal.style.display = "none";
    }
    else if (event.target == (modal = document.getElementById('algorithm'))){
      modal.style.display = "none";
    }
    else if (event.target == (modal = document.getElementById('use'))){
    modal.style.display = "none";
    }
    else if (event.target == (modal = document.getElementById('tools'))){
      modal.style.display = "none";
    }
} 
